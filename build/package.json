{
  "name": "build",
  "version": "1.0.0",
  "description": "This creates the release build that becomes available as `build.zip` after every new release of the Builder thanks to Travis.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "adm-zip": "^0.4.13"
  }
}
